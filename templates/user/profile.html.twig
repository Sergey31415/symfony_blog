{% extends 'base.html.twig' %}

{% block title %} User Profile {% endblock %}

{% block body %}
<div class="col-6 px-5 py-4  mx-auto mt-4 border">
    <h3 class="text-center mb-4"> {{ user.name }} </h3>
    <img src="{{asset('build/images/no-user.png')}}" width="100%" class="mb-3">
  <p class="text-center mb-4">{{ user.email }}</p>
  <div class="d-flex justify-content-around">
    <a class="btn btn-primary text-light">edit profile</a>
    <a class="btn btn-primary text-light">delete profile</a>
  </div>
</div>
{% endblock %}